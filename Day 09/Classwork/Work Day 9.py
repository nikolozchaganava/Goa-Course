#So we are going to make a code about salaries
salary = float(input("What's your monthly salary?: "))
print("1 month came past and your balance is " + str(salary) + "$")

#They have you the 20% raise and now we will code that
raise_salary = salary * 1.20
print("Second month came past and your balance is "+ str(raise_salary) + "$")